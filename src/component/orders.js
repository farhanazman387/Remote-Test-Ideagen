import React, { Component } from 'react';

class Order extends Component{
    render(){
        return(
            <h1>
                Table in progress
            </h1>
        );
    }
}

export default Order;